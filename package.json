{
  "name": "aoc-twinevia-saas-browser-tests",
  "private": true,
  "scripts": {
    "playwright:install": "playwright install chromium",
    "test:browser": "playwright test",
    "test:browser:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
